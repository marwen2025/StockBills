version: '3'
services:
  backend:
    image: stockbillsbackend:latest
    ports:
      - "5000:5000"

  frontend:
    image: stockbillsfrontend:latest
    ports:
      - "3000:3000"
    depends_on:
      - backend
  
  